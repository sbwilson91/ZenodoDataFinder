name: Zenodo Bot

on:
  # Monday 07:00 UTC — dataset discovery to start the week
  schedule:
    - cron: "0 7 * * 1"
  workflow_dispatch:

permissions:
  contents: write

jobs:
  zenodo-bot:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Run Zenodo Bot
        env:
          PYTHONPATH:      zenodo_bot
          GOOGLE_API_KEY:  ${{ secrets.GOOGLE_API_KEY }}
          EMAIL_SENDER:    ${{ secrets.EMAIL_SENDER }}
          EMAIL_RECEIVER:  ${{ secrets.EMAIL_RECEIVER }}
          EMAIL_PASSWORD:  ${{ secrets.EMAIL_PASSWORD }}
          LOOKBACK_PERIOD: week
        run: python zenodo_bot/zenodo_bot.py

      - name: Write bot status
        if: success()
        run: |
          mkdir -p docs
          python - <<'EOF'
          import json, datetime
          from pathlib import Path
          f = Path("docs/status.json")
          status = json.loads(f.read_text()) if f.exists() else {}
          status["zenodo_bot"] = {"last_run": datetime.date.today().isoformat(), "status": "success"}
          f.write_text(json.dumps(status, indent=2))
          EOF

      - name: Commit status
        if: success()
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add docs/status.json
          git diff --staged --quiet || git commit -m "status: zenodo $(date +%Y-%m-%d) [skip ci]"
          git push
